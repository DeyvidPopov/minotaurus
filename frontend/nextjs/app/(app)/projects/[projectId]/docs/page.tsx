"use client";
import { StubPage } from "@/components/ui/stub-page";

export default function DocsListPage() {
  return (
    <StubPage
      title="Documentation lives on each artifact"
      description="There is no project-wide docs index in this build. Open any artifact, then click the Documentation tab — Markdown editor with live preview, one page per artifact."
    />
  );
}
