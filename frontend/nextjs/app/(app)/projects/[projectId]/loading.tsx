// Route-segment loading UI for a project's overview (and the nearest fallback for
// sub-routes without their own loading.tsx). Re-exports the colocated skeleton so
// the route-transition and in-page `!project` branch render the same layout.
export { default } from "./skeleton";
