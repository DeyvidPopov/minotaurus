// Route-segment loading UI for the projects list. Re-exports the colocated
// skeleton so the route-transition fallback and the in-page `data === null`
// branch render the identical layout-matching skeleton.
export { default } from "./skeleton";
